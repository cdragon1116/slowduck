<% if message.head == true %>
<%= link_to '# ', chatroom_post_path(message.chatroom_id, message.post.id)%>
<% else %>
<%= link_to '- ', chatroom_post_path(message.chatroom_id, message.post.id)%>
<% end %>

<%= message.user.username%>:
<%= message.body %><br>